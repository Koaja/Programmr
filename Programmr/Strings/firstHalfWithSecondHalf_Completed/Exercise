Given a string from user, swap the first half with second half.
If the string length is odd, take the lower first half and swap. And if the string length is 1, leave it alone.

For Example:
Input:
abcde 
Output:
cdeab

Input:
abcd
Output:
cdab